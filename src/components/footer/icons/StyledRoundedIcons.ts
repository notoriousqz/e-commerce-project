import React from 'react';
import { styled } from '@mui/material';

export const RoundedIconButton = styled('div')({
    width: '40px',
    height: '40px',
    backgroundColor: '#424242',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: '50%',
});
