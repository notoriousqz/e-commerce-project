import { styled, Typography } from '@mui/material';

export const LogoTypography = styled(Typography)({
    display: 'flex',
    alignItems: 'center',
    fontStyle: 'normal',
    fontWeight: '600',
    fontSize: '24px',
    lineHeight: '30px',
    marginRight: 30,
    cursor: 'pointer',
    color: 'inherit',
});
